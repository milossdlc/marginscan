async function request(path:string,method:string,body?:unknown,params?:Record<string,unknown>){const url=new URL(path,window.location.origin);for(const [key,value]of Object.entries(params||{}))url.searchParams.set(key,String(value));const response=await fetch(url,{method,credentials:'same-origin',headers:body===undefined?{}:{'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw Object.assign(new Error(data.error||'Request failed'),{status:response.status});return {data};}
export const api={get:(path:string,params?:Record<string,unknown>)=>request(path,'GET',undefined,params),post:(path:string,body?:unknown)=>request(path,'POST',body)};
export const auth={async getUser(){return (await api.get('/api/auth/user')).data.user;},async signIn(){window.location.assign('/api/auth/login');return {user:null};},async signOut(){window.location.assign('/cdn-cgi/access/logout');}};
